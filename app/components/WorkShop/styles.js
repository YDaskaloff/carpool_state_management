import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  workShop: {
    height: 50,
    width: '30%',
    borderWidth: 1,
    borderColor: '#333',
    borderRadius: 15,
    backgroundColor: '#fff',
    paddingVertical: 5,
    paddingHorizontal: 10,
    alignItems: 'center',
  },
});
